# FsVoice.Ctx.Runtime

Oracle-style context runtime for FsVoice.

This package composes retrieval context, tools, durable memory, and answer transports to produce grounded context-backed answers. Blackboard data structures are available as optional utilities, but the default QA session does not maintain or expose a blackboard memory.
